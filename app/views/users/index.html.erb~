<ul class="breadcrumb">
  <li>
    <%= link_to raw("<i class='icon-home'></i> 首页"), root_path%> <span class="divider"><b>»</b></span>
  </li>
  <li class="active">用户列表</li>
</ul>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>序号</th>
      <th>用户名</th>
      <th>邮箱</th>
      <th>上次登录信息</th>
      <th>用户角色</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.id %></td>
        <td><%= link_to user.username, user_path(user) %></td>
        <td><%= user.email %></td>
        <td><%= user.last_sign_in_ip %></br><%= user.last_sign_in_atto_formatted_s(:db) %></td>
        <td><%= user.role %></td>
        <td><%= user.created_at %></td>
        <td>
          <%= link_to '编辑', edit_user_path(user), :class => 'btn btn-mini' %>
          <%= link_to '删除', user_path(user), :method => :delete, :confirm => '确认删除？！', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to '新增用户', new_user_path, :class => 'btn' %>
