<%= simple_form_for @supplier, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Supplier</legend>
<b>名称</b>
    <%= f.input :name   %>
    <%= f.input :address %>
    <%= f.input :mobile %>
    <%= f.input :email %>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to '取消', suppliers_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
