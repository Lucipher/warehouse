<ul class="breadcrumb">
  <li>
    <%= link_to raw("<i class='icon-home'></i>首页"), root_path%> <span class="divider">/</span>
  </li>
  <li class="active">供应商列表</li>
</ul>

<table class="table table-condensed table-bordered">
  <thead>
    <tr>
      <th>编号</th>
      <th>名称</th>
      <th>邮箱</th>
      <th>电话</th>
      <th>地址</th>
      <th>操作</th>
    </tr>
  </thead>
  <tbody>
    <% @suppliers.each do |supplier| %>
      <tr>
        <td><%= supplier.id %></td>
        <td><%= link_to supplier.name, supplier_path(supplier) %></td>
        <td><%= supplier.email %></td>
        <td><%= supplier.mobile %></td>
        <td><%= supplier.address %></td>
        <td>
          <%= link_to '编辑', edit_supplier_path(supplier), :class => 'btn btn-mini' %>
          <%= link_to '删除', supplier_path(supplier), :method => :delete, :confirm => '确定删除？！', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to '添加', new_supplier_path, :class => 'btn' %>
